<!DOCTYPE html>
<html>
  <head>
    <title>Codewars</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="/lib/codemirror.js"></script>
    <link rel="stylesheet" href="/lib/codemirror.css">
    <script src="/lib/javascript.js"></script>
  </head>
  <body>
    <h1>Codewars</h1>
    <h2>전투 훈련 본격 시작!</h2>

    <div class="problem">
      <div><%= problem.title %></div>
      <div>완성한 사람: <%= problem.completed_users %></div>
      <div>난이도: <%= problem.difficulty_level %></div>
      <div><%= problem.description %></div>
    </div>
    <form method="POST" action="../problems/<%= problem._id%>" class="code-form">
        <textarea id="code-area" name="code"></textarea>
        <input class="code-submit" type="submit" value="submit" />
      </form>
  </body>
<script>
    let editor = CodeMirror.fromTextArea(document.getElementById('code-area'), {
      mode: 'javascript',
      lineNumbers: true,
      linkWrapping: true,
      matchBrackets: true
    });
    editor.getDoc().setValue(`function soultion(a){\n  return a;\n}`);
    editor.setSize(null, 460);
  </script>
</html>
